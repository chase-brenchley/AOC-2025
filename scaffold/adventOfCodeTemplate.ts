export function partA(input: string): string[] {
  // Implement Part A logic for Day {{DAY_NUMBER}} here
  const lines = input.trim().split('\n')
  return lines;
}

export function partB(input: string): string[] {
  // Implement Part B logic for Day {{DAY_NUMBER}} here
  const lines = input.trim().split('\n')
  return lines;
}